#include "minishell.h"

/**
 * -> Check if relativ or absolut path 
 * 		- Find the "HOME" and fiter the path
 * 	- Compare the pathname if in the 
*/
void buid_cd (void)
{

	printf("Build cd\n"); 
}
